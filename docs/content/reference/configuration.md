---
title: "Configuration"
description: "Environment variables and global flags."
weight: 20
---

sinanews aims to need almost no configuration. As you add settings,
document each one here with its default.

## Global flags

| Flag | Meaning |
|---|---|
| `--help` | Help for any command |
| `--version` | Print the version |

## Environment variables

None yet. Add them here as the tool grows, prefixed `sinanews_` in upper
case (for example `sinanews_TOKEN`).
